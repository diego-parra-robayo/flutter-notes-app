name: notes
description: Package to handle notes main feature.
publish_to: 'none'
version: 0.0.1

environment:
  sdk: '>=2.18.6 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../core
  ui:
    path: ../ui

  cupertino_icons: ^1.0.2
  get_it: ^7.2.0
  fpdart: ^0.4.0
  go_router: ^6.0.1
  cloud_firestore: ^4.3.1
  flutter_bloc: ^8.1.1
  equatable: ^2.0.5
  freezed_annotation: ^2.2.0
  json_serializable: ^6.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  build_runner: ^2.3.3
  freezed: ^2.3.2

flutter:
  uses-material-design: true