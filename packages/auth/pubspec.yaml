name: auth
description: Package to handle authentication.
publish_to: 'none'
version: 0.0.1

environment:
  sdk: '>=2.19.2 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../core
  redux_core:
    path: ../redux_core
  ui:
    path: ../ui
  locale:
    path: ../locale

  cupertino_icons: ^1.0.5
  get_it: ^7.2.0
  go_router: ^6.0.7
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
